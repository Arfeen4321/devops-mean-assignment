name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend Image
        run: docker build -t arfeen3421/backend:latest ./backend

      - name: Build Frontend Image
        run: docker build -t arfeen3421/frontend:latest ./frontend

      - name: Push Backend Image
        run: docker push arfeen3421/backend:latest

      - name: Push Frontend Image
        run: docker push arfeen3421/frontend:latest

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ubuntu/devops-assignment/crud-dd-task-mean-app
            docker-compose pull
            docker-compose down
            docker-compose up -d
